# Force call-based stack probing — Rust 1.85+ uses inline probing by default,
# which removes the __rust_probestack symbol. wasmer_vm still references it.
[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "probe-stack=call"]
